[project]
name = "comprende-ya"
version = "0.1.0"
description = "Spanish learning app powered by local AI"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["voice-agent", "comprende-ya-mcp"]

[dependency-groups]
dev = ["ruff>=0.8.0", "mypy>=1.13.0", "pre-commit>=4.0.0"]
