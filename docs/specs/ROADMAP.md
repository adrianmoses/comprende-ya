# Roadmap

<!-- status: inferred -->
| Field | Value |
|---|---|
| status | approved |
| created | 2026-05-02 |

## Features

| ID  | Feature                                              | Status        | Spec |
|-----|------------------------------------------------------|---------------|------|
| 001 | YouTube search                                       | implemented   | —    |
| 002 | Pre-process dialect classification (transcript sample) | implemented | —    |
| 003 | Async video processing pipeline (Prefect flow)       | implemented   | —    |
| 004 | Whisper timestamped transcription                    | implemented   | —    |
| 005 | Claude-generated timestamped MCQs (5 per video)      | implemented   | —    |
| 006 | Transcript-segment extraction & storage              | implemented   | —    |
| 007 | spaCy fill-in-the-blank exercise generation          | implemented   | —    |
| 008 | Video CRUD endpoints (list, get-by-youtube-id)       | implemented   | —    |
| 009 | MCQ answer-progress tracking (single-user)           | implemented   | —    |
| 010 | Post-process dialect classification (full transcript) | implemented  | —    |
| 011 | Synchronous `/process` endpoint (legacy)             | deprecated    | —    |
| 012 | Persist flow status (replace in-memory `flow_runs` dict with a `processing_jobs` table) | implemented | [012-flow-status-persistence/spec.md](./012-flow-status-persistence/spec.md) |
| 013 | Frontend: project setup & shared shell (rail + topbar + theme tokens) | implemented | [013-frontend-project-setup/spec.md](./013-frontend-project-setup/spec.md) |
| 014 | Frontend: Inicio (library + KPIs)                    | implemented   | [014-inicio/spec.md](./014-inicio/spec.md) |
| 015 | Frontend: Escuchando (video, scrubber, transcript, MCQ rail) | implemented | [015-escuchando/spec.md](./015-escuchando/spec.md) |
| 016 | Frontend: Phrase Autopsy side panel                  | implemented   | [016-phrase-autopsy/spec.md](./016-phrase-autopsy/spec.md) |
| 017 | Backend: Phrase Autopsy data model + Claude generator | implemented | [017-phrase-autopsy-data-model/spec.md](./017-phrase-autopsy-data-model/spec.md) |
| 018 | Backend: token-level transcript with "interesting word" annotations | implemented | [018-token-level-transcript/spec.md](./018-token-level-transcript/spec.md) |
| 019 | Backend: chunk library schema + endpoints            | implemented   | [019-chunk-library/spec.md](./019-chunk-library/spec.md) |
| 020 | Frontend: Mis frases (chunk library + speaking prompts) | implemented | [020-mis-frases/spec.md](./020-mis-frases/spec.md) |
| 021 | Backend: speaking-prompt audio recording upload + playback API | implemented | [021-speaking-prompt-recordings/spec.md](./021-speaking-prompt-recordings/spec.md) · [decision.md](./021-speaking-prompt-recordings/decision.md) |
| 022 | Backend: user profile + streak + weekly KPIs         | in-progress   | [022-user-profile-kpis/spec.md](./022-user-profile-kpis/spec.md) |
| 023 | Tweaks panel (theme, accent, transcript size, level badges) | planned | —    |
| 024 | Configurable difficulty for fill-in-blank exercises  | planned       | —    |
| 025 | Test infrastructure + ruff bootstrap (pytest + DB fixture + CI) | implemented | [025-test-infrastructure/spec.md](./025-test-infrastructure/spec.md) |
| 026 | Refresh Anthropic model IDs to current (Opus 4.7 / Sonnet 4.6 / Haiku 4.5) | planned | — |
| 027 | La Libreta permalinks (ratify `youtube_id` as deep-link contract + `POST /api/videos/exists` seed validation) | implemented | [027-libreta-permalinks/spec.md](./027-libreta-permalinks/spec.md) |
| 028 | Frontend: speaking-prompt recording capture + playback (ChunkCard cutover) | implemented | [021 spec.md](./021-speaking-prompt-recordings/spec.md) · [decision-028](./021-speaking-prompt-recordings/decision-028-frontend.md) |
| 029 | Schema reset — models authoritative + squash to one Postgres-native baseline + test suite on Postgres (kills autogenerate drift) | implemented | [029-schema-reset-postgres-parity/spec.md](./029-schema-reset-postgres-parity/spec.md) · [decision.md](./029-schema-reset-postgres-parity/decision.md) |
| 030 | Track spaCy `es_dep_news_trf` + `torch` as managed deps (they live outside `uv.lock`, so every `uv sync`/`uv run` prunes them) | planned | — |

## Status Values

- `planned` — not yet started
- `in-progress` — spec written, implementation underway
- `implemented` — decision record complete
- `deprecated` — removed from product

## Revision History

| Date       | Change                                                      |
|------------|-------------------------------------------------------------|
| 2026-06-28 | 022 → `in-progress`. Spec drafted: backs Inicio's three placeholder KPIs (Esta semana / Racha / Comprensión) + greeting/profile chrome with real aggregates. Two new tables — singleton `profile` (name/level/created_at, seeded; 023 grows into it) + append-only `study_session`. New `/api/profile` router: `GET` (identity + all KPIs, one round trip), `POST /session` (listening heartbeat), `PUT` (edit name/level). Minutes via **real session tracking** (Escuchando posts PLAYING-state heartbeats) over a proxy; comprehension = **all-time** accuracy, nullable (`—` not `0 %`); streak = backward walk over distinct active days. **Full-stack** (Inicio wiring + heartbeat in scope). Confidence Medium — backend is small aggregate SQL; risk is the playback-time heartbeat (same class as 015's IFrame-player spike), validated by a ~30-min localhost spike. 3 Open Questions parked (Día semantics, timezone, MCQ-only-day-counts-as-active). Tests on Postgres (029 fixture); frontend stays manual-smoke. |
| 2026-05-02 | Initial roadmap inferred by audit skill. Backend features 001–010 marked `implemented`; legacy sync endpoint 011 marked `deprecated`; flow-status persistence (012) marked `in-progress` — the flow itself ships, only the polling surface needs a `processing_jobs` table. Frontend features 013–016, 020, 023 marked `planned` based on `docs/artefacts/` design bundle; backend follow-ups (017–019, 021, 022, 024, 025, 026) added as gaps surfaced in OVERVIEW audit notes. |
| 2026-05-02 | 012 → `implemented`. `processing_jobs` table replaces the in-memory dict; `/status` response trimmed (drops `result`, adds `youtube_video_id` + `video_id`); `/flows` paginates. Shipped without automated tests — 025 (test suite bootstrap) is the next prerequisite before further backend work. |
| 2026-05-02 | 025 → `in-progress`. Spec drafted: pytest + DB fixture (SQLite-first, Postgres-via-services-container as fallback) + monkeypatched service singletons + ruff (lint+format) + GitHub Actions. |
| 2026-05-02 | 025 → `implemented`. SQLite path landed (one dialect guard in migration `c6440d9b9453`); 17 tests across 4 files; ruff-clean codebase after audit-flagged cleanups (duplicate `import os`, unused `create_db_and_tables`, bare except). Decision record at `025-test-infrastructure/decision.md`. |
| 2026-05-03 | 013 → `in-progress`. Spec drafted: TanStack Start (recommended) vs plain Vite + TanStack Router (fallback) for the frontend toolchain; `webapp/` at repo root; rail + topbar + 4-theme tokens ported from the design bundle; dev port 3000 to match backend CORS; placeholder routes for Inicio / Escuchando / Mis frases; API smoke call to `GET /api/videos/`; no frontend tests yet (deferred). |
| 2026-05-03 | 013 → `implemented`. TanStack Start landed (no fallback needed). Tailwind v4 stripped post-scaffold (CLI forces it, but plain CSS is a clean replacement). SSR retained as default — eliminates FOUC for the `data-theme="dark"` attribute, deviates from spec §Non-Goals "client-rendered SPA" but justified in `decision.md`. Lint/typecheck/build all green. Decision record at `013-frontend-project-setup/decision.md`. |
| 2026-05-03 | 014 → `in-progress`. Spec drafted: Inicio page = greeting + 4 KPI placeholders + Continúa escuchando + Tu biblioteca, wired against `GET /api/videos/` and `GET /api/videos/{id}/progress`. KPI values stay as `—` / `0` until 022 lands real aggregates. Adds `@tanstack/react-query` for cross-screen data caching. Generated thumbnails (HSL hash) over YouTube thumbnails to keep payload lean and aesthetic consistent. |
| 2026-05-03 | 014 → `implemented`. `@tanstack/react-query` 5.100.9 wired with a single shared `QueryClient` in `__root.tsx`. `useQuery` for the videos list + `useQueries` for parallel per-video progress; cards link via the typed-params form `<Link to="/listen/$id" params={{ id: video.video_id }}>`. Fixed the `questions: number` → `Array<VideoQuestion>` type bug from 013 (only `routes/index.tsx` consumed it). Backend response uses `user_answer` (not `selected_answer`) and `video_id: string` (YouTube id, not DB id) — types corrected. Lint/typecheck/build green. Decision record at `014-inicio/decision.md`. |
| 2026-05-03 | 015 → `in-progress`. Spec drafted: Escuchando = real YouTube iframe player + scrubber with question marks + transport ±5s/speed + plain transcript with current-segment highlight + right rail with auto-pausing MCQ panel and always-on session panel. Wires against `GET /api/videos/{id}`, `GET /api/videos/{id}/segments` (DB id, not YouTube id — backend wart noted as Open Question), and `POST /api/videos/{id}/progress`. Phrase Autopsy / tappable words / save-phrase deferred to 016/020. Confidence Medium — two pre-impl spikes flagged: YouTube IFrame API + SSR hook, MCQ-due "fire once" effect logic. |
| 2026-05-03 | Architecture: **no English translations or glosses anywhere in the product.** Recorded as a permanent non-goal in OVERVIEW.md. Affects 015 (no "+ Inglés" toggle on the transcript), 016/017 (Phrase Autopsy data shape narrows to Spanish-only `{ grammar, natural_notes, register }` — drops the design's `natural`/`literal` English fields), 020 (Chunk Library entries Spanish-only). Reasoning: B2 learners should reason inside Spanish; English crutches regress them to L1-mediated comprehension. |
| 2026-05-03 | 015 → `implemented`. Real YouTube IFrame API embed via SSR-safe `useYouTubePlayer` hook (`@types/youtube` 0.2.0 added; `Window` augmentation lives in `src/types/youtube-window.d.ts` to dodge TS's UMD-in-module warning). Scrubber + transport + transcript with `is-current` highlight + auto-pausing MCQ panel + always-on session panel. First uses of `useMutation` and `useQueryClient` in the project; narrow invalidation on `['video-progress', youtubeId]` makes Inicio's progress bars update on back-navigate without a refetch flash. MCQ-due effect uses pending-latch + answered-set gating. Backend `/segments` int-id wart worked around via chained queries; cleanup deferred. Lint/typecheck/build green; SSR sanity (curl) clean. Decision record at `015-escuchando/decision.md`. |
| 2026-05-05 | 016 → `in-progress`. Spec drafted: frontend-only Phrase Autopsy side panel, fixture-backed (`webapp/src/data/autopsy-fixtures.ts`), with a temporary "Frases destacadas" list trigger in the right rail (clearly marked `temporal · ver 018`) until tappable transcript words land in 018. Two layers (Gramática, Por qué suena natural) — the design's English "Significado natural" layer is permanently dropped. Save toggle is session-local until 020. Aside priority: MCQ wins over autopsy. Confidence High. |
| 2026-05-06 | 016 → `implemented`. AutopsyPanel + AutopsyTriggerCard ship; fixture seeds two phrases on `m1DFpkNdcv0`. Two deliberate spec deviations: (1) reused the existing `.btn` system in `shell.css` (extended with `.primary` and `.accent` modifiers ported from the design) instead of autopsy-scoped `.btn-save`/`.btn-replay` — converges button vocabulary for 020. (2) Replaced the spec's IIFE-in-JSX pattern with a `const autopsyEntry` above the JSX, matching the existing `pendingQuestion` shape. Lint/typecheck/build green; SSR smoke (curl) renders the trigger card pre-hydration. Decision record at `016-phrase-autopsy/decision.md`. |
| 2026-05-09 | 017 → `in-progress`. Spec drafted: on-demand-per-tap caching model (no pre-generation in `process_video_flow`); `POST /api/videos/{id}/autopsy/explain` calls Claude on cache miss and persists to a new `phrase_autopsy` table, keyed `(video_id, normalized_phrase)`; `GET /api/videos/{id}/autopsy` lists cached entries. Spanish-only data shape (no English `natural`/`literal`). Confidence Low until a 5–10 phrase spike validates Claude's output shape; rises to Medium after. 016 stays on its frontend fixture until 018 ships tappable words — 017 alone doesn't replace it. |
| 2026-05-09 | 018 → `in-progress`. Spec drafted: phrase-span markers stored as a JSON `tokens` column on `VideoSegment`; one Claude call per video at `process_video_flow` time produces the markers AND pre-warms 017's `phrase_autopsy` cache (combined prompt, single round-trip). `GET /segments` extended additively to return `tokens`. Frontend wiring in scope: tappable spans replace the temporary "Frases destacadas" trigger; `webapp/src/data/autopsy-fixtures.ts` and `AutopsyTriggerCard` are deleted. `ON CONFLICT DO NOTHING` on cache pre-warm so manual taps win over batch generation. Tappable unmarked words explicitly out of scope (free-form vocabulary lookup is a separate feature). Confidence Low until a 1–2 video spike validates contiguity rate + editorial quality; rises to Medium after. |
| 2026-05-13 | 019 → `in-progress`. Spec drafted: `chunks` table + `/api/chunks` router (POST save / GET list / DELETE). Standalone schema — no FK to `phrase_autopsy`. Claude generates 2-4 Spanish speaking prompts at save time, persisted on the row (write-once cache, same pattern as 017). No `mastery` column — deferred until 021 (recordings) gives a real signal. Cache key `(video_id, phrase_key)` reuses 017's `normalize_phrase`. Confidence Low until a 5-10-phrase spike confirms the prompt produces invitational (not analytic) output; rises to Medium after. |
| 2026-05-14 | 019 spec → `approved`. Prompt spike passed: 7 phrases × 2 runs = 14 calls against `claude-4-sonnet-20250514`; 14/14 JSON+validator clean, 28/28 consignas invitational (not analytic), 28/28 Spanish-only, no within-run near-duplicates. Cross-run scenario repetition is mild and invisible in production (single-shot rendering). Bare-filler `«pues»` produced clean output — concern was misplaced. Borderline: `«¿sabes?»` run 2 drifted reflective; schema-safe, a prompt-only iteration target if it surfaces. Confidence Low → Medium. Spike script ran locally and was not committed (017 pattern). |
| 2026-05-16 | 020 → `in-progress`. Spec drafted: maximalist visual port of `screens-home.jsx:74-165` against 019's `/api/chunks`. Mis frases route renders library cards (phrase, source link, prompt rotation, recording stub, mastery dots) + filter chips. Deferred-feature affordances ship as honest visual stubs (mastery 0/5, "Necesitan práctica" disabled, "Empezar sesión" no-op, recording = 3.5s timer with no audio capture) tagged `temporal · ver 021`. Three out-of-route wirings folded in: AutopsyPanel save toggle swaps `Set<string>` for real `POST`/`DELETE` mutations against `["chunks"]`; Inicio "Frases guardadas" KPI wires to `GET /api/chunks` count; `/listen/$id` gains a `?t={start_time}` deep-link (validateSearch + one-shot seek-on-ready effect) for the jump-to-source affordance. No in-screen delete on cards — unsave is via AutopsyPanel where save originated. Confidence Medium → High after a 15-min spike confirms the deep-link race + the empty-body 204 fix to `api()` wrapper. |
| 2026-05-15 | 019 → `implemented`. `chunks` table + `/api/chunks` router (POST 201 / GET / DELETE 204) + `ChunkPromptsService` + `ChunkRepository`. Locked prompt is the spike's V1 verbatim. Post-`/simplify` cleanup collapsed `ChunkRepository.to_response(row)` from 3 args → 1 arg and added `selectinload(Chunk.video)` to `list_all()` — kills a latent N+1 on `GET /api/chunks` and removes route-layer ORM-relationship traversal. Known drift from 017's `AutopsyRepository.to_response(row, youtube_id)` which still has the leaky shape; flagged as a cleanup follow-up. `seeded_video` fixture lifted to `tests/conftest.py` (was duplicated byte-for-byte in `test_chunks_routes.py` + `test_autopsy_routes.py`). 28 new tests, 97 total passing. Ruff lint + format clean. Migration applies and reverts cleanly. Two Open Questions (length cap 200 vs 80, normalization aggressiveness) stay parked until 020 + production usage. Decision record at `019-chunk-library/decision.md`. |
| 2026-05-17 | 020 → `implemented`. Mis frases route + ChunkCard + ChunksFilterRow + ChunksEmptyState + `mis-frases.css` port; `AutopsyPanel` save toggle rewired from session-local `Set<string>` to real `POST`/`DELETE` mutations against a shared `["chunks"]` query (memoized `Map<normalized-phrase, Chunk>` keyed on `[chunksQuery.data, youtubeId]` for O(1) `isSaved` lookups); Inicio "Frases guardadas" KPI + Rail badge both read the same query (react-query dedupes across 4 call sites). `/listen/$id` got `validateSearch` for `?t=number` + one-shot seek-on-ready effect; spike confirmed no autoplay-block from same-tab source-link gesture. Shared `api()` wrapper got the empty-body 204 fix. Three `/simplify` deviations from spec code samples: dropped dead `"low"` filter union variant (never dispatched); extracted `<MisFrasesShell>` wrapper and inlined `ChunksEmptyState` into `chunks.tsx`; replaced user-visible "ver 021" tooltip leaks with "Próximamente". `pnpm lint` + `typecheck` + `build` clean; 019 backend 28/28 passing; SSR sanity (`curl /chunks` → 200, route match `s:"success",ssr:!0`). Decision record at `020-mis-frases/decision.md`. |
| 2026-06-21 | 027 → `implemented`. `POST /api/videos/exists` (`{ids} → {present, missing}`, order-preserving) + `VideoRepository.existing_youtube_ids` (single `youtube_id IN (…)` projection, empty-list guard) + `VideoExistsRequest` schema. OQ1 resolved to (d) batch-existence over (b) `/ids` enumeration — membership test, seed-sized transfer, no pagination, no route-ordering trap. No new table/migration (read-only on `videos`). Ratified `youtube_id` as the external permalink contract (no slug) in `ARCHITECTURE.md` "External Contracts" + README; companion-interface.md updated in the La Libreta repo. 5 new tests (partition, order, empty-ids, >20 no-truncation, 404-permalink-miss regression); 102 total passing; ruff check + format clean. Decision record at `027-libreta-permalinks/decision.md`. |
| 2026-06-28 | 029 → `implemented`. Models made authoritative (FK `ondelete`, `unique_video_question_progress`, the discovered `ck_processing_jobs_status` CHECK, `error`→TEXT via `sa_column`/`__table_args__`); 11 migrations squashed into one Postgres-native baseline (`1b329637734b`, `down_revision=None`) regenerated from the models; test suite moved onto real Postgres. **Headline test passes**: `upgrade head` + `autogenerate` → 0 operations. Two deviations from spec: (1) **testcontainers dropped for `DATABASE_URL_TEST`** — no Docker in the dev env, CI uses a services-postgres, net-zero dependency (within OQ1's allowed envelope); (2) **discovered + preserved the migration-only `status` CHECK constraint** the spec didn't enumerate (guarded by `test_db_fixture.py`). Isolation via per-test `TRUNCATE … RESTART IDENTITY CASCADE` (repos commit, so rollback won't do). 114 tests green on Postgres; ruff clean; `pyproject`/`uv.lock` unchanged. Surfaced a pre-existing fragility → new item **030** (the spaCy model + torch live outside `uv.lock`, so `uv sync` prunes them). Decision record at `029-schema-reset-postgres-parity/decision.md`. |
| 2026-06-27 | 029 → `in-progress`. Spec drafted: kill the `alembic autogenerate` drift at the root. Reproduced against a fresh SQLite (built from the migrations themselves) — the SQLModel classes under-declare FK `ondelete`, the `unique_video_question_progress` constraint, and some indexes that the migrations built, so autogenerate re-proposes them forever; the SQLite-tests / Postgres-prod split adds dialect noise (`TEXT` vs `VARCHAR`, the `c6440d9b9453` dialect guard) on top. Chosen the **full reset** (over a reconciling patch migration) since it's local-only test data: (1) make models authoritative, (2) squash all 8 migrations to one Postgres-native baseline regenerated from the corrected models, (3) move the test suite from in-memory SQLite to Postgres (testcontainers, leaning) + a CI Postgres service. Keep the unique constraint (the `save_answer` upsert relies on it); `error`→TEXT by aligning the model down (no column alter). Definitive success test: `alembic upgrade head` + `alembic revision --autogenerate` → empty diff against Postgres, all 114 tests green on the new fixture. Confidence Medium — moves 1–2 mechanical, move 3 (fixture redesign + Docker/CI) is the surface. Sequenced before 022 so the next table-adding feature builds on a trustworthy baseline. Decision record after implementation. |
| 2026-06-27 | 028 → `implemented`. Frontend cutover: `ChunkCard.tsx`'s `REC_STUB_MS` timer replaced with real capture via a new `useAudioRecorder` hook (`getUserMedia` + `MediaRecorder`, default mimeType, 30s auto-stop, track cleanup, `denied`/`unsupported` states); stop fires a react-query upload mutation → `POST …/recording` → invalidates `["chunks"]`; native `<audio controls>` playback + Regrabar/Borrar when `has_recording`. Shared `api()` wrapper now skips the JSON content-type for `FormData` bodies. All browser-API access kept in handlers → SSR-safe (verified via build + `curl /chunks`). **Backend bug found in smoke + fixed**: the recording `GET` had no cache headers, so the browser replayed stale audio after re-record/reload — added `Cache-Control: no-store` + a regression test (`test_get_is_not_browser_cacheable`). The frontend `?v=` cache-bust is kept only to force the `<audio>` element to re-request on in-session re-record, not for caching. 114 backend tests passing; `pnpm lint`/`typecheck`/`build` green. Frontend stays manual-smoke (`getUserMedia` can't run headless). Completes the 021 recording arc. Decision record at `021-speaking-prompt-recordings/decision-028-frontend.md`. |
| 2026-06-27 | 021 → `implemented` (backend). `recordings` table (filesystem + DB path, unique on `chunk_id`, overwrite-on-re-record) + `POST`/`GET`/`DELETE /api/chunks/{id}/recording` on the existing chunks router + `RecordingRepository` + `recording_storage` helper (server-generated uuid filenames → traversal-safe) + `has_recording` on `ChunkResponse` via `selectinload(Chunk.recording)` (no N+1). Chunk-delete unlinks the audio file before the row cascades. **OQ1 gate passed**: `MediaRecorder`'s `audio/webm;codecs=opus` round-trips through POST→disk→GET→`<audio>` and plays back with no transcoding — store-as-received holds; the documented `mimeType`-pin fallback wasn't needed. Validated in two halves (backend byte-path proven programmatically with a real ffmpeg opus/webm; browser playback via a throwaway same-origin `/spike` page, since removed). 11 new tests, 113 total passing; ruff check + format clean. Migration hand-stripped to only the `recordings` table — autogenerate had pulled in unrelated drift on `answer_progress`/`frase_exercise`/`processing_jobs` (flagged as a cleanup candidate). Scope was backend-only per the approved plan; the frontend cutover (replace `ChunkCard.tsx`'s `REC_STUB_MS` stub with real capture + playback) is split out as new item **028**, now unblocked. Decision record at `021-speaking-prompt-recordings/decision.md`. |
| 2026-06-27 | 021 → `in-progress`. Spec drafted: closes the chunk-library speaking loop by capturing/replaying the learner's own audio against a saved chunk's *consignas*. Scope is backend **+** frontend cutover (replaces 020's `REC_STUB_MS` timer stub in `ChunkCard.tsx` with real `MediaRecorder` capture). Storage = filesystem (`recordings/` dir, sibling of `temp/`, durable not transient) + a new `recordings` table holding path/content-type/size, unique on `chunk_id` (one take per chunk, overwrite-on-re-record). New routes on the existing chunks router (`POST`/`GET`/`DELETE /api/chunks/{id}/recording`); `ChunkResponse` gains `has_recording` via `selectinload(Chunk.recording)` to dodge an N+1. Permanent non-goal reaffirmed: **no ASR/pronunciation scoring** — recordings are personal-playback only (OVERVIEW "No audio evaluation"). Confidence Medium — backend is a 019-shaped near-copy; the unknown is the browser `MediaRecorder` round-trip (format varies by browser; store-as-received, serve-as-is). Validate via a thin record→POST→GET→`<audio>` spike on `localhost` before wiring the full card; rises to High after. Backend gets pytest coverage; frontend stays manual-smoke (`getUserMedia` can't run headless). |
| 2026-05-10 | 018 → `implemented`. JSON `tokens` column on `VideoSegment` + `PhraseMarkersService` + `tokenize_segment` helper + 2 Prefect tasks between `save_video_segments` and `generate_exercises_task`. Pre-warm uses app-level `get_by_phrase` check (not raw `ON CONFLICT`) to match `AutopsyRepository`'s commit-per-row pattern; semantic identical. Shared `src/services/spanish_nlp.py` `get_nlp()` singleton — only the new code consumes it; `FraseExerciseGeneratorService` refactor deferred to 026. `scripts/backfill_phrase_markers.py` added (committed) so pre-018 videos can be made tappable without re-transcription — a deliberate divergence from spec's "no backfill" Non-Goal (the Non-Goal stands for prod; the script is a dev tool). Spike done live via the backfill against `m1DFpkNdcv0`: 15/15 markers parsed, **14/15 = 93% located** (spec bar was ≥80%), one silent drop (`«dar a luz»` mis-attributed by Claude to seg 5). 28 new tests, 69 total passing; ruff/format/typecheck/lint clean. Decision record at `018-token-level-transcript/decision.md`. |
